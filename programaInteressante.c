#include <stdio.h>

int sequencia[5] = {2, 3, 5, 4, 1};
int sequenciaOrdenada[5];
int main()
{
    int tamanho = 5;
    sequenciaOrdenada[0] = sequencia[0]
    for(i = 0; i < tamanho; i++)
    {
        if (sequencia[i] > sequenciaOrdenada[i-1]) sequenciaOrdenada[i] = sequencia[i];
        if (sequencia[i] < sequenciaOrdenada[0])
        {
            for(int j; j < i; j++)
            {

            }
        }
    }
}
